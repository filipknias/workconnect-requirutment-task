# shadcn/ui components import peer deps (react, @base-ui/react) directly, so the
# strict pnpm store must hoist nothing silently — keep resolution explicit.
auto-install-peers=true
strict-peer-dependencies=false
